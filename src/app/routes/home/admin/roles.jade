.ui.form
  .fields
    .field
      .ui.right.action.left.icon.input
        i.search.icon
        input(type="text", placeholder="Search", (input)="setKeyword($event.target.value)")
        .ui.basic.floating.dropdown.button
          .text Select field
          i.dropdown.icon
          .menu
            .item((click)="setField('')") All
            .item((click)="setField('name')") Name

table.ui.definition.compact.celled.table
  thead
    tr
      th
      th Name
      th.one.wide
      th.one.wide
  tbody
    tr(*ngFor="#t of list | async")
      td.collapsing
        .ui.fitted.checkbox
          input(type="checkbox", (change)="select(t, $event.target.checked)", [ngModel]="select(t)")
          label
      td {{t.name}}
      td.center.aligned
        .ui.teal.icon.button((click)="edit(t)")
          i.edit.icon
      td.center.aligned
        .ui.red.icon.button((click)="delete(t)")
          i.remove.icon
  tfoot.full-width
    tr
      th
      th(colspan=3)
        .ui.right.floated.small.primary.labeled.icon.button((click)="add()")
          i.users.icon
          | Add Role
        .ui.red.small.labeled.icon.button((click)="deleteSelected()")
          i.erase.icon
          | Delete Selected

.ui.right.aligned.grid
  pagination.column(*ngIf="!loading", [data]="page", [subscribeTo]="observable", (next)="next($event)")

.ui.active.centered.large.inline.loader(*ngIf="loading")

#roleForm.ui.long.modal
  .header {{header}}
  .content
    form.ui.form([ngFormModel]="model")
      .field([class.error]="model.controls['name'].touched && !model.controls['name'].valid")
        label Name:
        input(ngControl="name", type="text")
      .field
        label Collections:
        input(type="text")
      .field
        table.ui.striped.celled.definition.table
          thead
            tr
              th.one.wide
              th Name
              th.one.wide C
              th.one.wide R
              th.one.wide U
              th.one.wide D
              th.one.wide A
          tbody
            tr
              td.collapsing
                .ui.fitted.checkbox
                  input(type="checkbox")
                  label
              td aa
              td
                .ui.fitted.checkbox
                  input(type="checkbox")
                  label
              td
                .ui.fitted.checkbox
                  input(type="checkbox")
                  label
              td
                .ui.fitted.checkbox
                  input(type="checkbox")
                  label
              td
                .ui.fitted.checkbox
                  input(type="checkbox")
                  label
              td
                .ui.fitted.checkbox
                  input(type="checkbox")
                  label
  .actions
    .negative.ui.button
      i.remove.icon
      | Cancel
    .positive.ui.button((click)="submit()")
      i.checkmark.icon
      | OK
      
.alert([subscribeTo]="observable", (next)="next($event)")
